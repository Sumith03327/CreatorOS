import type { Skill } from './types';

export const channelStrategy: Skill = {
  name: 'channel-strategy',
  title: 'Channel Strategy',
  description: 'Positioning, content pillars, upload cadence, and how to plan a calendar that compounds.',
  whenToUse: 'When planning a content calendar, choosing what to make next, or advising on channel direction.',
  content: `# Channel Strategy

Growth comes from **repeatable formats aimed at a specific viewer**, not from volume.

## Positioning
Answer in one sentence: *"I help [specific person] get [specific outcome] by [format]."*
If the sentence needs an "and", the channel is two channels.

A channel that could belong to anyone belongs to no one. Narrow beats broad —
you can widen after the audience exists, never before.

## Content pillars
Pick **3 pillars** the channel returns to. Each pillar should have a repeatable format.
Formats compound: the audience learns what to expect, and the algorithm learns who to show it to.

Balance every month roughly:
- **60% proven** — the format that already works. This pays the bills.
- **30% adjacent** — a variation on the proven format (new angle, new stake).
- **10% experiment** — a genuinely new format. Expect most to fail; one will define the next year.

Never run more than one experiment at a time; you won't know what caused the result.

## Cadence
- **Consistency beats frequency.** One good video a week beats three rushed ones.
- Cadence should be the maximum you can sustain for 6 months without dropping quality.
- Sub-1000 subs: prioritise *iteration speed* over polish — you're searching for the format.
- Post-format-fit: prioritise *quality* — you're compounding an audience.

## Choosing the next video
Score each idea 1–5 on:
1. **Audience pull** — do they already want this? (search volume, comments, DMs)
2. **Proven format** — can I execute a shape that's worked before?
3. **Differentiation** — can I say something others can't?
4. **Effort** — realistically, can I finish it well?

Make the highest **(pull × differentiation) / effort**. Ignore ideas that only excite you.

## Calendar construction
For a 30-day plan:
- Anchor each week with one **proven-format** video (the reliable one).
- Add one **adjacent** piece per week if cadence allows.
- Slot the **experiment** mid-month, never in a launch week.
- Leave one buffer slot. Something will go wrong.

For each planned upload specify: date, working title, format (long/Short), the hook angle,
and the **goal** (growth / retention / monetisation). A video without a goal is a hobby.

## Reading the signal
- A video that over-performs is a **format signal**, not a topic signal. Ask what *shape* worked.
- Two failures in a row on the same format = the format is done, not the channel.
- Never judge a video before 14 days. Never change strategy on a sample size of one.
`,
};
